<div class="papers">
  {{#each papers}}
  <article class="card">
    <div class="card-header flex-row justify-space-between">
      <h2>
        <a href="/paper/{{id}}">{{title}}</a>
      </h2>
    </div>
    <div class="card-body">
        {{subject}}
    </div>
  </article>
  {{/each}}
</div>